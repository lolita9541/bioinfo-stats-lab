CMS 升級包

請把這個資料夾裡面的 admin、content、assets、README-CMS.txt 上傳到 GitHub repo 根目錄。
不要把 bioinfo-stats-cms-upgrade 這一層資料夾上傳。

這一步不會覆蓋目前首頁。

重要：上傳後 /admin/ 介面會存在，但 GitHub 登入還需要設定 OAuth：
GitHub OAuth App + Netlify Authentication Provider。

CMS 目前管理 Markdown 內容；下一階段才把首頁改成自動呈現 CMS 內容。
